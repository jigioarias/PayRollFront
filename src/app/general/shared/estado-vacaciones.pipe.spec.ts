import { EstadoVacacionesPipe } from './estado-vacaciones.pipe';

describe('EstadoVacacionesPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoVacacionesPipe();
    expect(pipe).toBeTruthy();
  });
});
