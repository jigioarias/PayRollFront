import { PipeSalaryTypePipe } from './pipe-salary-type.pipe';

describe('PipeSalaryTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeSalaryTypePipe();
    expect(pipe).toBeTruthy();
  });
});
